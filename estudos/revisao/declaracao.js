// Declaração
var name

// Atribuição de valores
// name 'recebe' Jamil Filho
var name = 'Jamil Filho'
console.log(name)

// checando o tipo de dado
console.log(typeof name)

// Agrupamento de Declarações
let age, isHuman

// Setando os valores
age = 25
isHuman = true

// Para imprimir múltiplos argumentos no console.logo utilizar vírgula
console.log(name,age,isHuman)

// Concatenação de valores
console.log('O ' + name + ' tem ' + age + ' anos')

// Interpolação de valores (template literals, template strings, ``)
console.log(`O ${name} tem ${age} anos`)