// Objetos

const person = { // objeto
    name: 'Jamil', // propriedade name
    age: 25, // propriedade age
    weight: 80, // propriedade weight
    isAdmin: true // propriedade adm
}

// Imprimindo o valor da propriedade 'name'
console.log(`${person.name} tem ${person.age} anos`)